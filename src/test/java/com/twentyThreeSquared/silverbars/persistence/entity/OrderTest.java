package com.twentyThreeSquared.silverbars.persistence.entity;

public class OrderTest {

    // Opted not to test the entity class because it's just a POJO and testing it
    // doesn't really provide much value other than Sonar coverage.

    // If I were serialising/deserialising it with Jackson and/or
    // using a builder then I'd test it.
}
